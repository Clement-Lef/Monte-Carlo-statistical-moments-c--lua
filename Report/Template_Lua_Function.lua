-- [[ To use a Lua function follow the following template, use the C++ array syntax to
-- get the coordinate ( e.g x = (x[1], x[2], ... x[n]).
-- Make sure that the function is called "func".
function func(x)
    return x[1]*x[2];
end
